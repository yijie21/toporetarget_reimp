__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/

# generated viewer payloads for licence-restricted datasets (regenerate locally)
viewer/data_contactpose_*.js
viewer/data_grab_*.js

# local dataset roots
data/
*.npz
!tests/fixtures/*.npz

# Run artefacts ARE committed: they are the evidence behind RESULTS.md (the full
# hyper-parameter sweeps and every per-grasp measurement), and they are small.
runs/_probe.json
